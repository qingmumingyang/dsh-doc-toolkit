# 依赖
node_modules/

# 日志
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 系统文件
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# 测试产物
test-output/
*.test.pdf
*.test.docx
*.test.xlsx
*.test.csv

# 测试用第三方 PDF 夹具：由 tests/oracle-pdf.test.mjs 按需下载（离线自动跳过），
# 不随仓库再分发第三方样例文件。
tests/fixtures/

# 说明：tests/（node:test 测试套件）刻意【不】忽略，随仓库发布；
# test-output/ 仅存放本地诊断脚本与临时产物。

# 说明：lib/（TypeScript 编译产物）刻意【不】忽略——
# 仓库随附预编译产物，用户从 GitHub 直接安装即可使用，无需本机执行 npm run build。
